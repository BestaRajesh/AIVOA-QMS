{
  "name": "aivoa-qms",
  "version": "1.0.0",
  "private": true,
  "description": "AIVOA QMS - AI-Powered Pharmaceutical Complaint Management System",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "backend": "backend\\venv\\Scripts\\python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000"
  }
}
